{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window: core APIs only (window theme, events) — this demonstrator's settings/export/timezone commands are plain Tauri commands, not plugin permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
